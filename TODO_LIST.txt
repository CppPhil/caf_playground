create tests using CAF test framework

use CAF all throughout the application, library and tests
    -> If it doesn't work for no reason at all
        -> Try using an object library
            -> If that doesn't work: Try to turn the library into a shared (dynamically linked) library 

Run CAF tests

Do versioning

Gather CAF resources

Set up CI

Try on WSL

